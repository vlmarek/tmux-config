Start with:
  ~/.tmux2/start-outer.sh

Keys:
  Ctrl-a c / Ctrl-a Ctrl-c   -> new INNER window
  Ctrl-a n / Ctrl-a p        -> next/prev INNER window

  Ctrl-x c / Ctrl-x Ctrl-c   -> new OUTER window (auto starts inner in it)
  Ctrl-x n / Ctrl-x p        -> next/prev OUTER window
